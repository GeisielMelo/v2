import React from "react";
import styled from "styled-components";

const Hero = () => {
  return (
    <section>
      <div>
        <p>Hero</p>
      </div>
    </section>
  );
};

export default Hero;
